interface:
  display_name: "Deploying Liferay"
  short_description: "Build, deploy, and verify Liferay changes in a local ldev runtime. Use when implementation exists and needs runtime proof."
  default_prompt: "Use $deploying-liferay to deploy this change locally and validate OSGi state and logs."
