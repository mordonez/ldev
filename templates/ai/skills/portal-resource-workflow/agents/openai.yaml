interface:
  display_name: "Portal Resource Workflow"
  short_description: "Export, import, and verify Liferay portal resources via ldev. Use when editing, importing, or exporting structures, templates, ADTs, or fragments."
  default_prompt: "Use $portal-resource-workflow to resolve, export, change, import, and verify Liferay structures, templates, ADTs, or fragments without guessing resource ownership."
