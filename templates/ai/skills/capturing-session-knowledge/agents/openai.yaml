interface:
  display_name: "Capturing Session Knowledge"
  short_description: "Distil session learnings into project-learnings.md to prevent repeated mistakes. Use at end of session after discovering non-obvious project constraints or failure causes."
  default_prompt: "Use $capturing-session-knowledge to distil verified session learnings into docs/ai/project-learnings.md only."
