# Repository-Backed Resource Workflow

This file is a compatibility pointer for older references.

Use `../../portal-resource-workflow/SKILL.md` as the canonical workflow for
structures, Journal templates, ADTs, and fragments. It owns:

- source-of-truth site resolution
- singular export before editing
- import vs migration classification
- `--check-only` where supported
- read-after-write verification
- browser validation for rendered behavior

Do not duplicate resource import command sequences here; update
`portal-resource-workflow` instead.
