# Playwright / Browser Testing Reference

Use `ldev` for runtime and portal discovery:

```bash
ldev context --json
ldev status --json
ldev liferay inventory page --url <fullUrl> --json
```

Use direct `playwright-cli` commands for browser control.
