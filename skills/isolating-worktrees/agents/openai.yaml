interface:
  display_name: "Isolating Worktrees"
  short_description: "Set up, lock, recover, and clean ldev-native isolated worktrees. Use when a task must not mutate the main checkout or needs its own runtime state."
  default_prompt: "Use $isolating-worktrees to set up or validate an isolated ldev-native worktree, keep edits inside the confirmed worktree root, and recover safely from worktree setup blockers."
